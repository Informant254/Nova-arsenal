apiVersion: v1
kind: Service
metadata:
  name: nova-llm
  namespace: nova-arsenal
  labels:
    app: nova-llm
spec:
  type: ClusterIP
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
  selector:
    app: nova-llm
