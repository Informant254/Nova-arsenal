apiVersion: v1
kind: Service
metadata:
  name: nova-agent
  namespace: nova-arsenal
  labels:
    app: nova-agent
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: nova-agent
